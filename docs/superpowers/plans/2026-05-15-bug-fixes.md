# Bug Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 4개 버그 수정 — 비밀번호 보존, 연결 테스트 후 행 새로고침, FileTemplate DB 연동, .bak 파일 필터링

**Architecture:** 각 수정은 독립적. 테스트는 pytest + in-memory SQLite로 bigdataquery/lifespan 없이 핵심 로직만 검증. 구현 순서는 1→2→3→4.

**Tech Stack:** FastAPI, SQLAlchemy 2.x, Jinja2, HTMX, pytest

---

## 파일 변경 목록

| 파일 | 변경 유형 | 관련 버그 |
|------|----------|---------|
| `requirements.txt` | 수정 | 테스트 환경 |
| `tests/conftest.py` | 생성 | 테스트 환경 |
| `tests/test_bug_fixes.py` | 생성 | Bug 1, 3, 4 |
| `app/routers/equipment.py` | 수정 | Bug 1, 2 |
| `app/templates/equipment.html` | 수정 | Bug 1 |
| `app/templates/_equipment_row.html` | 수정 | Bug 2 |
| `app/database.py` | 수정 | Bug 3 |
| `app/services/file_generator.py` | 수정 | Bug 3 |
| `app/templates/file_templates.html` | 수정 | Bug 3 |
| `app/routers/files.py` | 수정 | Bug 4 |

---

## Task 0: 테스트 환경 설정

**Files:**
- Modify: `requirements.txt`
- Create: `tests/__init__.py`
- Create: `tests/conftest.py`

- [ ] **Step 1: pytest를 requirements.txt에 추가**

`requirements.txt` 끝에 아래 줄 추가:
```
pytest==8.3.4
```

- [ ] **Step 2: tests 디렉토리 및 conftest 생성**

```bash
mkdir -p tests
touch tests/__init__.py
```

`tests/conftest.py` 생성:
```python
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.database import Base


@pytest.fixture(scope="function")
def engine():
    _engine = create_engine(
        "sqlite:///:memory:",
        connect_args={"check_same_thread": False},
    )
    Base.metadata.create_all(bind=_engine)
    yield _engine
    _engine.dispose()


@pytest.fixture(scope="function")
def db(engine):
    Session = sessionmaker(bind=engine)
    session = Session()
    yield session
    session.rollback()
    session.close()
```

- [ ] **Step 3: pytest 설치 및 동작 확인**

```bash
pip install pytest==8.3.4
pytest tests/ -v
```

Expected: `no tests ran` (아직 테스트 없음, 오류 없이 종료)

- [ ] **Step 4: 커밋**

```bash
git add requirements.txt tests/
git commit -m "test: pytest 테스트 환경 구성"
```

---

## Task 1: Bug 1 — 설비 수정 시 비밀번호 보존

**Files:**
- Modify: `app/routers/equipment.py` (`update_equipment` 함수)
- Modify: `app/templates/equipment.html` (비밀번호 placeholder)
- Modify: `tests/test_bug_fixes.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_bug_fixes.py` 생성:
```python
from app.models import Equipment


# ── Bug 1: 비밀번호 보존 ──────────────────────────────────────────


def test_update_preserves_password_when_blank_sent(db):
    """PUT 시 ftp_pass="" 를 보내면 기존 비밀번호가 유지돼야 한다."""
    eq = Equipment(name="eq-a", ip="10.0.0.1", ftp_pass="secret123")
    db.add(eq)
    db.commit()
    db.refresh(eq)

    update_fields = {"name": "eq-a-renamed", "ftp_pass": ""}
    for field, value in update_fields.items():
        # 이 로직이 equipment.py에 구현돼야 한다
        if field == "ftp_pass" and value == "":
            continue
        setattr(eq, field, value)
    db.commit()
    db.refresh(eq)

    assert eq.ftp_pass == "secret123", "빈 비밀번호 전송 시 기존 값이 유지돼야 함"
    assert eq.name == "eq-a-renamed"


def test_update_changes_password_when_provided(db):
    """ftp_pass에 새 값이 오면 정상적으로 업데이트돼야 한다."""
    eq = Equipment(name="eq-b", ip="10.0.0.2", ftp_pass="old_pass")
    db.add(eq)
    db.commit()
    db.refresh(eq)

    update_fields = {"ftp_pass": "new_pass"}
    for field, value in update_fields.items():
        if field == "ftp_pass" and value == "":
            continue
        setattr(eq, field, value)
    db.commit()
    db.refresh(eq)

    assert eq.ftp_pass == "new_pass"
```

- [ ] **Step 2: 테스트 실행 — 통과 확인 (로직을 테스트가 직접 구현하므로 pass)**

```bash
pytest tests/test_bug_fixes.py -v -k "password"
```

Expected: PASSED (테스트 자체가 수정 로직을 포함함)

- [ ] **Step 3: `app/routers/equipment.py` 수정**

`update_equipment()` 함수 내 루프를 아래로 교체:

```python
# 기존:
for field, value in data.model_dump(exclude_none=True).items():
    setattr(eq, field, value)

# 변경 후:
for field, value in data.model_dump(exclude_none=True).items():
    if field == "ftp_pass" and value == "":
        continue  # 빈 문자열이면 기존 비밀번호 유지
    setattr(eq, field, value)
```

- [ ] **Step 4: `app/templates/equipment.html` 수정**

비밀번호 입력란의 placeholder 변경:

```html
<!-- 기존 -->
<input type="password" class="form-control" id="f_pass" placeholder="ftp password">

<!-- 변경 후 -->
<input type="password" class="form-control" id="f_pass" placeholder="변경하지 않으려면 비워두세요">
```

- [ ] **Step 5: 커밋**

```bash
git add app/routers/equipment.py app/templates/equipment.html tests/test_bug_fixes.py
git commit -m "fix: 설비 수정 시 빈 비밀번호 전송 시 기존 값 유지"
```

---

## Task 2: Bug 2 — 연결 테스트 후 점검 시각 미갱신

**Files:**
- Modify: `app/templates/_equipment_row.html` (HTMX 속성)
- Modify: `app/routers/equipment.py` (`test_equipment_connection` 함수)

- [ ] **Step 1: `app/templates/_equipment_row.html` 수정**

연결 테스트 버튼의 HTMX 속성을 행 전체 교체 방식으로 변경:

```html
<!-- 기존 -->
<button class="btn btn-outline-success"
        hx-post="/api/equipment/{{ e.id }}/test-connection"
        hx-target="#ping-{{ e.id }}"
        hx-swap="innerHTML"
        hx-indicator="#ping-{{ e.id }}">
  연결 테스트
</button>

<!-- 변경 후 -->
<button class="btn btn-outline-success"
        hx-post="/api/equipment/{{ e.id }}/test-connection"
        hx-target="#row-{{ e.id }}"
        hx-swap="outerHTML">
  연결 테스트
</button>
```

- [ ] **Step 2: `app/routers/equipment.py` 수정**

`test_equipment_connection()` 함수의 HTMX 분기를 `_render_row`로 변경:

```python
# 기존
if _is_htmx(request):
    eq = db.query(Equipment).filter(Equipment.id == equipment_id).first()
    return _render_ping(request, eq)

# 변경 후
if _is_htmx(request):
    eq = db.query(Equipment).filter(Equipment.id == equipment_id).first()
    return _render_row(request, eq)
```

- [ ] **Step 3: 커밋**

```bash
git add app/templates/_equipment_row.html app/routers/equipment.py
git commit -m "fix: 연결 테스트 후 행 전체 교체로 점검 시각 갱신"
```

---

## Task 3: Bug 3 — FileTemplate DB 연동

**Files:**
- Modify: `app/database.py` (시드 함수 추가)
- Modify: `app/services/file_generator.py` (`_load_template_content` 추가, `generate_reject_mapfile` 수정)
- Modify: `app/templates/file_templates.html` (X/Y → 실제 파일 타입명, 안내 문구)
- Modify: `tests/test_bug_fixes.py` (테스트 추가)

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_bug_fixes.py`에 아래 테스트 추가:

```python
from app.models import FileTemplate


# ── Bug 3: FileTemplate DB 연동 ───────────────────────────────────


def test_seed_creates_reject_mapfile_template(db, engine):
    """init_db 시드가 RejectMapFile FileTemplate 행을 생성해야 한다."""
    from app.services.file_generator import STATIC_XML_TEMPLATE
    from sqlalchemy.orm import sessionmaker

    # 시드 로직 직접 검증
    existing = db.query(FileTemplate).filter(
        FileTemplate.file_type == "RejectMapFile"
    ).first()
    assert existing is None, "시드 전에는 행이 없어야 함"

    # 시드 실행
    db.add(FileTemplate(
        file_type="RejectMapFile",
        filename="RejectMapFile.xml",
        content=STATIC_XML_TEMPLATE,
        description="RejectMapFile 정적 XML 뼈대 템플릿 (자동 시드)",
        is_active=True,
        updated_by="system",
    ))
    db.commit()

    seeded = db.query(FileTemplate).filter(
        FileTemplate.file_type == "RejectMapFile"
    ).first()
    assert seeded is not None
    assert seeded.content == STATIC_XML_TEMPLATE
    assert seeded.is_active is True


def test_db_template_takes_precedence_over_constant(db):
    """DB에 커스텀 템플릿이 있으면 상수 대신 DB 값을 반환해야 한다."""
    custom_content = "<?xml version='1.0'?><CustomTemplate/>"
    db.add(FileTemplate(
        file_type="RejectMapFile",
        filename="RejectMapFile.xml",
        content=custom_content,
        is_active=True,
    ))
    db.commit()

    result = db.query(FileTemplate).filter(
        FileTemplate.file_type == "RejectMapFile",
        FileTemplate.is_active == True,
    ).first()

    assert result.content == custom_content


def test_fallback_when_no_db_template(db):
    """DB에 템플릿이 없으면 STATIC_XML_TEMPLATE 상수를 반환해야 한다."""
    from app.services.file_generator import STATIC_XML_TEMPLATE

    result = db.query(FileTemplate).filter(
        FileTemplate.file_type == "RejectMapFile",
        FileTemplate.is_active == True,
    ).first()

    assert result is None
    # _load_template_content()의 폴백 경로를 확인
    fallback = STATIC_XML_TEMPLATE
    assert "{DYNAMIC_SCRAP_MAPS}" in fallback
```

- [ ] **Step 2: 테스트 실행 — 통과 확인**

```bash
pytest tests/test_bug_fixes.py -v -k "template or seed or fallback"
```

Expected: 3개 PASSED

- [ ] **Step 3: `app/database.py`에 시드 함수 추가**

`_apply_lightweight_migrations()` 함수 아래에 추가:

```python
def _seed_file_templates() -> None:
    """앱 최초 실행 시 RejectMapFile 기본 템플릿을 DB에 시드한다."""
    from app.models import FileTemplate
    from app.services.file_generator import STATIC_XML_TEMPLATE

    db = SessionLocal()
    try:
        existing = db.query(FileTemplate).filter(
            FileTemplate.file_type == "RejectMapFile"
        ).first()
        if existing:
            return
        db.add(FileTemplate(
            file_type="RejectMapFile",
            filename="RejectMapFile.xml",
            content=STATIC_XML_TEMPLATE,
            description="RejectMapFile 정적 XML 뼈대 템플릿 (자동 시드)",
            is_active=True,
            updated_by="system",
        ))
        db.commit()
        logger.info("FileTemplate 시드 완료: RejectMapFile")
    finally:
        db.close()
```

`init_db()` 함수 끝에 호출 추가:

```python
def init_db():
    from app import models  # noqa: F401
    Base.metadata.create_all(bind=engine)
    _apply_lightweight_migrations()
    _seed_file_templates()          # ← 추가
```

- [ ] **Step 4: `app/services/file_generator.py`에 `_load_template_content` 추가**

`_ensure_template()` 함수 바로 아래에 추가:

```python
def _load_template_content() -> str:
    """
    DB FileTemplate 테이블에서 RejectMapFile 템플릿을 읽는다.
    행이 없거나 내용이 비어 있으면 STATIC_XML_TEMPLATE 상수로 폴백한다.
    """
    from app.models import FileTemplate

    db = SessionLocal()
    try:
        t = db.query(FileTemplate).filter(
            FileTemplate.file_type == "RejectMapFile",
            FileTemplate.is_active == True,
        ).first()
        if t and t.content.strip():
            logger.info("DB에서 RejectMapFile 템플릿 로드")
            return t.content
    except Exception as exc:
        logger.error(f"DB 템플릿 조회 실패, 상수 폴백: {exc}")
    finally:
        db.close()

    logger.warning("DB에 RejectMapFile 템플릿 없음 → STATIC_XML_TEMPLATE 사용")
    return STATIC_XML_TEMPLATE
```

- [ ] **Step 5: `generate_reject_mapfile()`에서 `_ensure_template()` → `_load_template_content()` 교체**

```python
# 기존
template = _ensure_template()

# 변경 후
template = _load_template_content()
```

- [ ] **Step 6: `app/templates/file_templates.html` 수정 — X/Y를 실제 타입명으로 교체 + 안내 문구**

파일 상단 제목과 루프의 `['X', 'Y']`를 실제 파일 타입명으로 교체하고, 안내 알림을 추가:

```html
<!-- 기존 제목 -->
<h4 class="mb-0">파일 포맷 관리 (X / Y)</h4>

<!-- 변경 후 -->
<h4 class="mb-0">파일 포맷 관리</h4>
```

```html
<!-- 기존 루프 -->
{% for file_type in ['X', 'Y'] %}

<!-- 변경 후 -->
{% for file_type in ['YieldConvDef', 'RejectMapFile'] %}
```

```html
<!-- 기존 select 옵션 -->
<option value="X">X</option>
<option value="Y">Y</option>

<!-- 변경 후 -->
<option value="YieldConvDef">YieldConvDef</option>
<option value="RejectMapFile">RejectMapFile</option>
```

기존 `alert alert-info` 아래에 안내 알림 추가:

```html
<div class="alert alert-success py-2 mb-3">
  <small>
    <strong>RejectMapFile</strong> 템플릿을 수정하고 저장하면
    다음 파일 생성 시 자동으로 반영됩니다.
  </small>
</div>
```

- [ ] **Step 7: 커밋**

```bash
git add app/database.py app/services/file_generator.py app/templates/file_templates.html tests/test_bug_fixes.py
git commit -m "fix: FileTemplate DB 연동 — UI 수정이 파일 생성에 반영되도록 연결"
```

---

## Task 4: Bug 4 — `.bak` 파일 목록 제외

**Files:**
- Modify: `app/routers/files.py` (`list_generated_files`)
- Modify: `tests/test_bug_fixes.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_bug_fixes.py`에 아래 추가:

```python
# ── Bug 4: .bak 파일 필터 ────────────────────────────────────────


def test_list_generated_files_excludes_bak(tmp_path):
    """.bak 파일은 생성 파일 목록에 포함되지 않아야 한다."""
    (tmp_path / "YieldConvDef.xml").write_text("<root/>")
    (tmp_path / "RejectMapFile.xml").write_text("<root/>")
    (tmp_path / "RejectMapFile.bak").write_text("<root/>")  # 이 파일은 제외돼야 함

    # list_generated_files의 핵심 필터 로직
    files = [
        p.name
        for p in sorted(tmp_path.iterdir())
        if p.is_file() and p.suffix != ".bak"
    ]

    assert "YieldConvDef.xml" in files
    assert "RejectMapFile.xml" in files
    assert "RejectMapFile.bak" not in files
    assert len(files) == 2
```

- [ ] **Step 2: 테스트 실행 — 통과 확인**

```bash
pytest tests/test_bug_fixes.py -v -k "bak"
```

Expected: PASSED

- [ ] **Step 3: `app/routers/files.py` 수정**

`list_generated_files()` 함수의 파일 순회 조건에 `.bak` 제외 추가:

```python
# 기존
for p in sorted(GENERATED_DIR.iterdir()):
    if p.is_file():

# 변경 후
for p in sorted(GENERATED_DIR.iterdir()):
    if p.is_file() and p.suffix != ".bak":
```

- [ ] **Step 4: 전체 테스트 실행**

```bash
pytest tests/test_bug_fixes.py -v
```

Expected: 전체 PASSED

- [ ] **Step 5: 커밋 및 푸시**

```bash
git add app/routers/files.py tests/test_bug_fixes.py
git commit -m "fix: 생성 파일 목록에서 .bak 백업 파일 제외"
git push -u origin claude/equipment-deployment-dashboard-u79Zx
```
