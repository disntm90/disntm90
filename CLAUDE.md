# Claude Code — 프로젝트 지침

## 주식 종목 분석

종목 분석 요청 시 (`XXX 분석해줘`, `XXX 종목 분석`, 등) 반드시 아래 파일을 적용:

**→ `/home/user/disntm90/stock-analysis-template.md` 참고**

### 시각화 파일 저장 규칙
- 파일명: `dashboard-{TICKER}-{YYYYMMDD}.html` (소문자, 날짜 포함)
  - 예: `dashboard-slnh-20260511.html`, `dashboard-nvda-20260511.html`
  - 날짜는 분석 요청 당일 기준 (currentDate 사용)
- 저장 위치: `/home/user/disntm90/`
- **덮어쓰기 금지**: 같은 티커+날짜 파일이 이미 존재하면 `dashboard-{TICKER}-{YYYYMMDD}-v2.html` 형식으로 버전 증가
- gh-pages 배포 시: GitHub API(`mcp__github__create_or_update_file`)로 `{ticker}-{YYYYMMDD}.html` 경로에 push
  - URL: `https://disntm90.github.io/disntm90/{ticker}-{YYYYMMDD}.html`
  - **배포하면 인덱스에 자동 반영** (별도 작업 불필요)

### 열람 인덱스 (자동 업데이트)
- URL: **`https://disntm90.github.io/disntm90/`**
- `index.html`이 GitHub Contents API로 `{ticker}-{YYYYMMDD}.html` 파일을 자동 감지
- 신규 종목을 gh-pages에 배포하면 인덱스에 즉시 반영됨 (수동 편집 불필요)
- 인덱스 로컬 파일: `/home/user/disntm90/index.html`

### 대시보드 필수 요소
각 대시보드 헤더에 반드시 포함:
```jsx
<a href="https://disntm90.github.io/disntm90/" style={{...}}>← 목록으로</a>
```

### 분석 순서
1. 웹 검색으로 실시간 가격·지표 수집
2. `stock-analysis-template.md` 7개 섹션 순서대로 분석 작성
3. 분석 완료 후 인터랙티브 React 대시보드 생성 → `dashboard-{TICKER}-{YYYYMMDD}.html`
4. GitHub API로 gh-pages에 배포 → 인덱스 자동 반영
5. 개별 대시보드 URL + 인덱스 URL 유저에게 안내

### 언어 & 포맷
- 한국어, 맞춤법 확인
- 모바일 단일 컬럼 (화면 분할 금지)
- 차트: Chart.js (CDN), 다크 테마

---

## 종목 발굴

트리거: `종목발굴`, `종목 발굴`, `종목발굴해줘`, `종목 발굴해줘` 등 "종목발굴" 키워드 포함 시 반드시 아래 절차 적용:

### 발굴 절차

**Step 1 — 분야 질문 (필수)**
트리거 감지 즉시 아래 형식으로 분야를 질문한다. 절대 먼저 종목을 제시하지 말 것.

```
어떤 분야에서 종목을 발굴할까요?

예시: AI 반도체, 바이오/신약, 재생에너지, 핀테크, 우주항공,
      사이버보안, 소형주 전체, 부동산(리츠), 원자재, 기타(직접 입력)
```

**Step 2 — 발굴 철학: 밸류체인 탑다운 분석 (필수 적용)**

> 이 프로젝트의 종목 발굴은 단순 재무 스크리닝이 아니라,
> **탑다운(Top-Down) × 밸류체인(Value Chain) 분석**을 결합한 방식으로 수행한다.
> 목표: "남들이 다 아는 완제품·표면 수혜주"가 아닌,
> **그 완제품을 만들기 위해 반드시 쥐고 흔들어야 하는 숨겨진 핵심 노드(병목 기업)** 를 선점한다.

---

### Step 2-Pre — 기술 심층 배경 분석 (웹검색 필수, 밸류체인 분석 전 선행)

분야 확정 즉시, 밸류체인 분석 전에 아래 4가지를 **웹 검색으로 반드시 수집·정리**한다.
이 단계는 "왜 지금 이 분야인가"를 기술적·시장적 근거로 증명하는 역할을 한다.

**① 기존 기술의 근본 한계 (3가지 서술 원칙)**
- 해당 분야에서 기존 기술이 AI 시대에 맞닥뜨린 **물리적·구조적 한계**를 3가지로 구체화
- 각 한계는 "왜 AI 시대에 더 심각해졌는가"를 함께 서술
- 예) 구리 배선:
  - 대역폭 한계 — GPU 수천 개 연결 시 전기 신호 전송 속도 한계
  - 발열·전력 — 데이터 전송 자체에서 발생하는 열과 전력 낭비 급증
  - 확장성 부재 — 수백 Tbps 이상 요구하는 미래 AI 시스템에 대응 불가

**② 신기술 우위성 — 왜 대체 불가인가 (정량 비교 포함)**
- 신기술이 ①의 한계를 어떻게 극복하는지 **구체적 수치**와 함께 서술
- 신호 손실률·전력 효율·대역폭·비용 등 기존 기술 대비 정량 비교
- 예) 실리콘 포토닉스: 장거리 신호 손실 거의 없음, 동일 대역폭 대비 전력 소모 90% 절감

**③ 결정적 흐름 — 3단계 타임라인 (연도 포함)**

아래 형식으로 해당 분야가 주목받게 된 흐름을 3단계로 정리한다:

```
1단계 — [초기 수요 폭발] (YYYY~YYYY)
  어떤 사건/기술이 이 분야의 수요를 촉발했는가
  → AI/빅테크의 어떤 문제가 이 기술을 필수로 만들었는가

2단계 — [빅테크 전략 투자] (YYYY~YYYY)
  어떤 대형 플레이어(NVDA·MSFT·Google·Meta 등)가 왜 진입했는가
  → 수십억 달러 계약·인수·R&D 확대 사례 구체 서술

3단계 — [시장 폭증·기술 성숙] (YYYY~현재~미래)
  현재 시장 규모($B), YoY 성장률(%), 미래 전망
  → CPO·모듈 통합 등 기술 완성도가 높아진 결정적 변곡점
```

**④ 시장 규모 데이터 (숫자로 제시, 출처 포함)**

| 항목 | 수치 |
|------|------|
| 현재 시장 규모 | $XB (YYYY 기준) |
| YoY 성장률 | +X% |
| CAGR | X% (YYYY~YYYY) |
| 목표 시장 규모 | $XB (YYYY 전망) |

> 이 4가지 분석 결과는 대시보드 **🔬 기술분석** 탭에 그대로 반영한다.

---

### Step 2-L — 레이어 탐색 프로세스 (L1~L5)

> 핵심 철학: "그들이 뭘 샀느냐"가 아니라,
> **"어떻게 질문하고, 어떻게 배제하고, 어떻게 소란을 검증 가능한 단계들로 분해하는가"**

밸류체인 분석 실행 시 아래 5단계 레이어 탐색 순서를 반드시 따른다.

**L1 — 터미널(Terminal) 먼저 확인**
- "어떤 기계·시스템·제품이 대량 생산에 들어가는가?"를 먼저 정의
- 종목을 먼저 찾지 않는다. 터미널이 무엇인지 먼저 확정한다.
- 예) 휴머노이드 로봇 → 터미널 확인 후 공급망 탐색 시작

**L2 — 공급망 스택 그리기**
- 터미널이 만들어지기까지 누가 몇 층에 있는지 전체 스택을 그린다
- 상류(원자재·소재) → 중류(부품·장비·설계) → 하류(조립·시스템·서비스)
- 각 층에서 "대체 가능 vs 대체 불가" 플레이어를 구분한다

**L3 — 진짜 카 포인트(Choke Point) 찾기**
아래 6가지 병목 유형 중 실제로 막혀 있는 층을 찾는다:

| 병목 유형 | 확인 질문 |
|---------|---------|
| 생산량 | 수요 대비 공급 가능한가? 리드타임이 얼마인가? |
| 인증 | 규제·안전 인증이 필요한가? 진입에 수년이 걸리는가? |
| 열(Heat) | 발열 제거 없이는 시스템이 작동 불가능한가? |
| 전기(Power) | 전력 공급이 프로젝트의 병목인가? |
| 수율(Yield) | 불량률이 높아 수요를 못 따라가는가? |
| 재료 | 희소 원소·특수 소재가 공급을 제한하는가? |

> 💡 **옆으로 보기 원칙**: 한 층이 이미 주목받고 있다면, 동일하게 필수적이나 아직 가격이 덜 반영된 **옆 층**을 찾는다.
> 예) 하모닉 감속기(이미 주목) → 옆: 포스 센서(관절마다 필수, 아직 덜 주목)

**L4 — 재무·뉴스·리서치로 교차 검증**
L3에서 찾은 병목 기업을 아래 3가지로 반드시 교차 검증:
- **재무제표**: 수주잔고 증가, 매출 성장률, 마진 추이
- **산업 뉴스**: 공급 부족 기사, 고객사 언급, 계약 발표
- **리서치 리포트**: 애널리스트 목표가 상향, 커버리지 시작

**L5 — 방향 → 이름 → 포지션 순서 준수**
- **먼저**: "어느 층을 파야 하는가" (방향)
- **그다음**: "그 층에서 누가 핵심인가" (이름)
- **마지막**: "얼마나, 언제 진입할 것인가" (포지션)
- ⚠️ 이름부터 시작하면 확증 편향(Confirmation Bias)에 빠진다

#### 언섹시 층(Unsexy Layer) 우선 탐색 원칙

소란스럽지 않고, 허세 부리기에도 별로인 층이 진짜 병목인 경우가 많다.

| 언섹시 층 | 이유 | 예시 섹터 |
|---------|------|---------|
| 전력 공급 | 칩이 아무리 세도 전기 없으면 무용 | 변압기, 전력설비, 원자력 |
| 액체 냉각 | 캐비닛이 아무리 많아도 열 안 빠지면 중단 | CDU, 냉각 시스템 |
| 물리적 인프라 | 데이터센터 준공 지연 = 전체 프로젝트 대기 | 건설·설비·배송 |
| 인증·특수소재 | 교체 불가능, 취득에 수년 소요 | 희소소재, 규제 취득 기업 |

> "칩이 아무리 세도, 전기 안 들어가면 소용없어.
> 캐비닛이 아무리 많아도, 열 안 빠지면 소용없어.
> 모델이 아무리 똑똑해도, 데이터센터 안 나오면 줄 서야 해."

---

**4단계 작동 원리**

1. **메가 트렌드 분해 (Top-Down)**
   - 유저가 제시한 분야를 지배하는 거대 흐름을 먼저 파악한다.
   - Step 2-Pre에서 수집한 기술 배경·흐름이 이 단계의 근거가 된다.
   - 예) "AI" → AI 학습·추론 수요 폭증이 핵심 동력

2. **산업 밸류체인 해부 (Value Chain Analysis)**
   - 해당 트렌드가 실현되기 위한 상류(원자재·소재·인프라) → 중류(부품·제조·설비) → 하류(완제품·서비스·소비자) 전체를 추적한다.
   - 파생 논리를 끝까지 확장한다:
     > AI → GPU(엔비디아) → HBM 메모리 → 광 모듈 → 냉각·패키징 → 네트워크·저장소 → **전력(원자력)** 이 최종 핵심 노드

3. **경제적 해자를 갖춘 '핵심 노드' 발굴 (Finding the Moat)**
   아래 3조건을 **동시에** 만족하는 기업을 최우선으로 선발한다:
   - ① **대체 불가성**: 해당 기업 없이는 밸류체인 전체가 멈춤
   - ② **높은 진입 장벽**: 경쟁자가 단기간에 따라올 수 없는 기술·특허·규모
   - ③ **절대적 의존도**: 거대 기업(빅테크·대형 제조사 등)이 반드시 이 기업의 제품/서비스를 써야 함

4. **기술 병목 교차 검증 (Technical Bottleneck Cross-Check)**
   Step 2-Pre에서 도출한 기술 한계·결정적 흐름과 발굴 종목을 교차 검증한다:
   - 해당 종목이 "기술 한계를 해결하는 솔루션"의 핵심 공급자인가?
   - 결정적 흐름 3단계 중 어느 단계에 위치하는가? (초기→중기→성숙)
   - 빅테크 전략 투자 대상에 직·간접으로 포함되었는가?

**발굴 우선순위 원칙**
- 표면적 수혜주(1차 수혜)보다 **2차·3차 파생 핵심 노드**를 우선 발굴
- "모두가 이미 아는 종목"은 발굴 결과에서 제외하거나 최하순위로 배치
- 밸류체인 상에서 **병목(Bottleneck)** 구간에 위치한 기업일수록 높은 점수 부여
- **기술 성숙도 초기~중기** 단계 기업에 멀티플 확장 가능성이 집중됨 (성숙기는 이미 반영)
- **L3 카 포인트 기준**: 생산량·인증·열·전기·수율·재료 중 하나라도 막힌 기업 최우선
- **L5 순서 엄수**: 반드시 방향(층) → 이름(기업) → 포지션(진입) 순서로 제시

---

**Step 2-B — 정량 발굴 조건**
분야 답변 수신 후, 아래 기준으로 종목 탐색:

| 조건 | 기준 |
|------|------|
| 시가총액 | $5B 이하 우선 (멀티배거 가능성) |
| 멀티배거 점수 | Anna Yartseva 8개 항목 중 5점 이상 |
| 기술적 신호 | RSI 30~55 구간 (과매도 반등 or 중립) |
| 촉매 | 최근 3개월 내 실적 개선 or 뉴스 촉매 존재 |
| 유동성 | 일평균 거래량 $500K 이상 |

**Step 3 — 발굴 결과 제시**
- 웹 검색으로 해당 분야 밸류체인을 먼저 도식화한 뒤, 각 구간에서 유망 종목 **최소 10개 이상** 탐색
- **병목 유형별 최소 기준**: L3에서 도출한 각 병목 유형(전기·열·광·인증·재료·생산량 등)마다 **최소 1~2개** 종목을 발굴해야 함. 특정 병목 유형에 집중되지 않도록 **병목 다양성** 확보 필수
- 종목 수가 10개 미만이면 발굴이 충분하지 않은 것으로 간주하고 추가 탐색 수행
- 결과 상단에 **밸류체인 맵** 을 텍스트로 먼저 제시:

```
[메가 트렌드] → [상류] → [중류] → [하류]
                  ↑ 핵심 노드 표시 (★)
```

- 각 종목에 대해 아래 요약 카드 형식으로 제시:

```
🔍 [티커] — [기업명]
   현재가: $X.XX | 시가총액: $XB
   밸류체인 위치: 상류 / 중류 / 하류 (★핵심노드 여부)
   대체 불가성: 높음 / 보통 / 낮음
   핵심 포인트: (1줄)
   멀티배거 조건: X/8 충족
   주목 이유: (2~3줄) — 파생 논리 포함
```

- 결과 하단에 **"[티커] 분석해줘"** 로 바로 이어서 심층 분석 가능함을 안내

**Step 3-B — 종목발굴 대시보드 생성 & 배포 (필수)**
텍스트 결과 제시 후 반드시 인터랙티브 웹 대시보드를 생성하고 gh-pages에 배포한다.

#### 발굴 대시보드 파일명 규칙
- 로컬: `dashboard-{분야}-discovery-{YYYYMMDD}.html`
  - 예: `dashboard-space-discovery-20260529.html`, `dashboard-robotics-discovery-20260529.html`
  - 분야명은 영문 소문자 (space, robotics, biotech, fintech, defense, energy, cyber 등)
- gh-pages 배포: `{분야}-discovery-{YYYYMMDD}.html`
  - URL: `https://disntm90.github.io/disntm90/{분야}-discovery-{YYYYMMDD}.html`
  - **배포하면 인덱스에 자동 반영** (index.html이 `discovery` 포함 파일을 🔍 종목발굴 대시보드로 자동 표시)
- **덮어쓰기 금지**: 같은 분야+날짜 파일이 이미 존재하면 `-v2.html` 형식 버전 증가

#### 발굴 대시보드 필수 구성 (6탭)
1. **🗺️ 밸류체인** — 탑다운 밸류체인 맵 + 핵심 파생 논리
2. **🔬 기술분석** — 기존 기술 한계 / 신기술 우위성 / 결정적 흐름 3단계 타임라인 / 시장 규모 데이터
3. **🔍 발굴종목** — 종목별 카드 (가격·시총·RSI·핵심포인트·멀티배거 바)
4. **📊 비교차트** — 시가총액 / 멀티배거 점수 / RSI Chart.js 차트
5. **🔟 멀티배거** — Anna Yartseva 8항목 상세 (종목 선택 버튼)
6. **🎯 전략** — 우선순위 순위표 + 촉매/리스크 + 포트폴리오 제안

#### 발굴 대시보드 테마 색상 (분야별)
| 분야 | 테마 색 |
|------|---------|
| 우주항공(space) | `#7c3aed` (보라) |
| 로보틱스(robotics) | `#10b981` (초록) |
| AI/반도체 | `#3b82f6` (파랑) |
| 바이오(biotech) | `#ec4899` (핑크) |
| 에너지(energy) | `#f59e0b` (앰버) |
| 사이버보안(cyber) | `#ef4444` (빨강) |
| 데이터센터(datacenter) | `#06b6d4` (시안) |
| 비트코인채굴(cryptomining) | `#f97316` (주황) |
| 기타 | `#06b6d4` (시안) |

#### 발굴 대시보드 생성 후 순서
1. 로컬 파일 저장 (`/home/user/disntm90/dashboard-{분야}-discovery-{YYYYMMDD}.html`)
2. git worktree 방식으로 gh-pages 배포:
   ```bash
   cp /home/user/disntm90/dashboard-{분야}-discovery-{YYYYMMDD}.html /home/user/disntm90/.gh-pages/{분야}-discovery-{YYYYMMDD}.html
   cd /home/user/disntm90/.gh-pages
   git add {분야}-discovery-{YYYYMMDD}.html
   git commit -m "feat: {분야} 종목발굴 대시보드 {YYYYMMDD}"
   git pull --rebase origin gh-pages   # non-fast-forward 방지
   git push origin gh-pages
   ```
   > ⚠️ GitHub API(`mcp__github__create_or_update_file`)는 파일 크기 제한(40KB+)으로 사용 불가
3. 로컬 dev 브랜치에 커밋 & push (`git push -u origin claude/ai-stock-analysis-BG75V`)
4. 유저에게 대시보드 URL + 인덱스 URL 안내

**Step 4 — 심층 분석 연계**
유저가 발굴 결과 중 특정 종목을 선택하면 → 자동으로 `주식 종목 분석` 절차로 전환
(stock-analysis-template.md 7개 섹션 + 대시보드 생성 + gh-pages 배포)
